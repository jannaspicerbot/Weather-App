# Ambient Weather API Credentials
# Get your API and Application keys from: https://ambientweather.net/account
AMBIENT_API_KEY=your_api_key_here
AMBIENT_APP_KEY=your_application_key_here

# Database Configuration
# Path to DuckDB database file (optional, defaults to ./ambient_weather.duckdb)
# DB_PATH=/data/ambient_weather.duckdb

# Server Configuration
# Host and port for FastAPI server (optional)
# BIND_HOST=0.0.0.0
# BIND_PORT=8000

# Scheduler Configuration
# Enable/disable automatic data collection (default: true)
SCHEDULER_ENABLED=true

# Fetch interval in minutes (default: 5)
# How often to automatically fetch new weather data
SCHEDULER_FETCH_INTERVAL_MINUTES=5

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# LOG_LEVEL=INFO
